exports.config = {
  graphitePort: <%= graphite_port %>,
  graphiteHost: <%= graphite_host %>
  port: <%= statsd_port %>,
  lock_file: <%= lock_file %>
  log_file : <%= log_file %>
  flushInterval: <%= flush_interval %>,
};
